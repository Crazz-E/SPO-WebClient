{
  "name": "starpeace-online-client",
  "version": "1.1.0-beta",
  "description": "TypeScript web client for Starpeace Online - A browser-based multiplayer tycoon/simulation game",
  "main": "dist/server/server.js",
  "scripts": {
    "build:server": "tsc",
    "build:client": "vite build",
    "build:terrain-test": "esbuild src/client/terrain-test.ts --bundle --outfile=public/terrain-test.js --format=iife",
    "build:electron-bundle": "esbuild dist/server/server.js --bundle --platform=node --target=node18 --format=cjs --outfile=dist/server-bundle.js --external:electron --external:7zip-min --external:7zip-bin",
    "build": "npm run build:server && npm run build:client && npm run build:terrain-test",
    "start": "node --disable-warning=DEP0040 dist/server/server.js",
    "dev": "npm run build && npm start",
    "dev:react": "vite",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:verbose": "jest --verbose",
    "test:changed": "jest --onlyChanged --bail",
    "test:smoke": "jest --selectProjects component --bail",
    "release": "node scripts/release.js",
    "electron": "npm run build:server && cd electron && npm start",
    "electron:validate": "node scripts/validate-electron-package.js",
    "electron:dist": "npm run build && npm run build:electron-bundle && cd electron && npm run dist && cd .. && npm run electron:validate"
  },
  "keywords": [
    "starpeace",
    "online",
    "game",
    "multiplayer",
    "tycoon",
    "simulation",
    "websocket",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/Crazysky/SPO-WebClient/starpeace-online-client.git"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "7zip-min": "^2.1.0",
    "cheerio": "^1.1.2",
    "gifuct-js": "^2.1.2",
    "lucide-react": "^0.575.0",
    "node-fetch": "^2.7.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "ws": "^8.19.0",
    "zustand": "^5.0.11"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.9",
    "@types/node-fetch": "^2.6.12",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@types/ws": "^8.18.1",
    "@vitejs/plugin-react": "^5.1.4",
    "esbuild": "^0.27.2",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "optionalDependencies": {
    "webp-wasm": "^1.0.6"
  }
}
