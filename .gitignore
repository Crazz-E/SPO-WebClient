# Dependencies
node_modules/

# Build outputs
dist/
public/app.js
public/app.js.map
public/app*.css
public/terrain-test.js
public/assets/
public/.vite/

# Environment variables
.env
.env.local

# Logs
*.log
npm-debug.log*
logs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
nul
*.tmp

# Update server mirror (exact structure from update.starpeaceonline.com)
# Regenerated automatically at startup and on-demand
cache/

# WebClient-specific cache (runtime image proxy, objects, textures)
# Chunks now served from CDN (SPO-WebClient-Chunks)
webclient-cache/

# Test coverage
coverage/

# E2E session screenshots
screenshots/

# Playwright MCP session logs
.playwright-mcp/

# Claude AI local overrides (settings.local.json only - shared config is tracked)
.claude/settings.local.json
.claude/memory/

# Skill installer scripts (contain API key references)
.claude/skillsmp-installer.js
.claude/install-cdn-skills.js
.claude/install-new-skills.js
.claude/scheduled_tasks.lock
.claude/.typecheck-dirty

# Per-session Claude worktrees (each is a full checkout; never tracked)
.claude/worktrees/

# Legacy UI reference screenshots (local only)
UI_Legacy_screenshots/

# Live-gate evidence — per-machine, per-run; summarised into the PR body instead
report/e2e/
