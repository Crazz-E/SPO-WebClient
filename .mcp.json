{
  "mcpServers": {
    "playwright": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@playwright/mcp@latest"],
      "description": "Browser automation for E2E testing (login flows, world selection, building interactions)"
    },
    "github": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN:-}"
      },
      "description": "GitHub integration for PRs, issues, and code search (set GITHUB_PERSONAL_ACCESS_TOKEN env var)"
    },
    "context7": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation lookup for TypeScript, Jest, Node.js, WebSocket, and other libraries"
    }
  }
}
